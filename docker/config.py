# This is for reddit
client_id = '${BOT_CLIENT_ID}'
client_secret = '${BOT_CLIENT_SECRET}'
username = '${BOT_USERNAME}'
password = '${BOT_PASSWORD}'
user_agent = '${BOT_USER_AGENT}'

subreddits = ['${BOT_SUBREDDIT}']
name = '${BOT_NAME}'

dry_run = ${BOT_DRY_RUN}

dbconfig = {
    "host": "${MYSQL_HOST}",
    "port": ${MYSQL_PORT},
    "user": "${MYSQL_USER}",
    "password": "${MYSQL_PASSWORD}",
    "database": "${MYSQL_DATABASE}",
}
